class ImageData {
  final List<int> bytes;
  final int originalSize;
  final int reducedSize;

  ImageData({
    required this.bytes,
    required this.originalSize,
    required this.reducedSize,
  });
}
